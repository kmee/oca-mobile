import {StyleSheet} from 'react-native';
import {general} from '../../styles';

const styles = StyleSheet.create(general);

// Example of overriding gerenal;
// styles.ActivityIndicatorStyle.alignItems = 'flex-start';
// styles.ActivityIndicatorStyle.justifyContent = 'flex-start';

export default styles;
